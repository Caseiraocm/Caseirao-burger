function gerarPedido() {
    let nome = document.getElementById('nome').value;
    let endereco = document.getElementById('endereco').value;
    let pagamento = document.getElementById('pagamento').value;
    let taxaEntrega = "Taxa de entrega: R$ 5,00 dentro da cidade. Zona rural a combinar.";

    let mensagem = `Pedido:\nHambúrguer: ${nome}\nEndereço: ${endereco}\nForma de Pagamento: ${pagamento}\n${taxaEntrega}`;

    // Número do WhatsApp do Caseirão Burger no formato internacional
    let numeroWhatsApp = "5586995033988"; // Substitua pelo número correto

    // Gera o link do WhatsApp com a mensagem codificada
    let linkWhatsApp = `https://wa.me/${numeroWhatsApp}?text=${encodeURIComponent(mensagem)}`;

    console.log("Link gerado:", linkWhatsApp); // Verifique o link gerado no console para debug
    window.open(linkWhatsApp, '_blank'); // Abre o link em uma nova aba
}
